export enum TablePaginationPosition {
    TOP = 'top',
    BOTTOM = 'bottom'
}

export enum TableColumnTypes {
    NUMBER = 'number',
    TEXT = 'text',
    CALENDAR = 'calendar',
    CHECKBOX = 'checkbox',
    DROP_DOWN = 'dropdown',
    CUSTOM = 'custom'
}

export enum TableColumnTextAlignment {
    LEFT = 'left',
    CENTER = 'center',
    RIGHT = 'right'
}